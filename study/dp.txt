dynamic programming -> 동적 계획법
이전에 계산한 답을 유지해서 이후의 문제의 답을 구하는데 사용한다 -> 
배열을 이용해서 순서대로 값을 구하면서 배열에 저장 -> 이 저장된 값을 이용해 이후의 값을 구해갈수 있다 
= 점화식을 구성해서 배열에 어떤 순서로 저장할 것인지를 알아야한다.
이전의 값을 이용해 이후의 값을 계산함으로 이후의 값을 계산하는데 걸리는 시간이 급격히 감소한다.
-> 시간 제한이 빡센 문제에 써보는 것을 고려한다. 또한 점화식이 만들어지는 문제도 고려가능하다.
